name: Deploy to VPS

on:
  push:
    branches: [ prod ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H 79.143.190.190 >> ~/.ssh/known_hosts

      - name: Deploy
        run: |
          ssh -i ~/.ssh/deploy_key -T hans@79.143.190.190 \
            "cd /home/hans/Optic-et-distribution && \
             git pull origin prod && \
             source venv/bin/activate && \
             pip install -r requirements.txt --quiet && \
             DJANGO_SETTINGS_MODULE=config.settings.production python manage.py collectstatic --no-input && \
             sudo systemctl restart optic-vision && \
             echo 'Deploiement termine'"
